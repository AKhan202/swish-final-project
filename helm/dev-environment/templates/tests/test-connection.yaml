apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dev-environment.fullname" . }}-test-connection"
  labels:
    {{- include "dev-environment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dev-environment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
